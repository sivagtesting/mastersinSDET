package foundation.week11.day2;

public class ConsecutiveCharacters_1446 {
	
	/*
	 * https://leetcode.com/problems/consecutive-characters/submissions/
	 * 
	 * PseudoCode:
			check if size of input eqult to 1, if yes then return 1
			initialize left =0, right = 0, count = 1
			do the following using while until right < length of input s
			check if char at index left equal char at right index
			        if yes then increment right check if count gt right-left
			            if yes then count = right - left
			        if no then
			            set left = right
			return count  
	 * 
	 */

}
